<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <!-- Provider to auto configure Actito -->
        <provider
            android:name="com.actito.ActitoConfigurationProvider"
            android:authorities="${applicationId}.ActitoConfigurationProvider"
            android:exported="false" />

        <!-- Provider to auto configure the session module -->
        <provider
            android:name="com.actito.ActitoSessionConfigurationProvider"
            android:authorities="${applicationId}.ActitoSessionConfigurationProvider"
            android:exported="false" />

        <!-- Receiver for locale and timezone changes -->
        <receiver
            android:name="com.actito.ActitoSystemIntentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
